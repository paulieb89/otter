---
"@otter/core": minor
---

Initial agent loop: types, tool registry, gate pipeline (hooks → policy →
resolve → handler → watchdog), non-cooperative cancellation, trajectory
emitter, ModelAdapter interface, FakeAdapter, and conformance tests for
invariants 1–7.
